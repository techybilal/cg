#include<graphics.h>
#include<conio.h>
int main()
{
	int gd=DETECT, gm;
	initgraph(&gd,&gm, "c:\\Turboc3\\bgi");
	arc(150,150,0,135,50);
	outtextxy(150,130,"ARC");
	setcolor(YELLOW);	
	outtextxy(0,0,"Name: - ");
    outtextxy(0,30,"Roll.No: -");
	getch();
	closegraph();
	return 0;
}
